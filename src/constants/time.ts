/**
 * Time-related constants
 */

// Base time units
export const SECONDS_PER_MINUTE = 60;
export const MINUTES_PER_HOUR = 60;
export const SECONDS_PER_HOUR = 3600;

// Default time values
export const DEFAULT_FINISH_TIME_SECONDS = 1800; // 30 minutes

// Time limits for running
export const MIN_FINISH_TIME_SECONDS = 300; // 5 minutes
export const MAX_FINISH_TIME_SECONDS = 21600; // 6 hours

// Exercise time ranges
export const MIN_HOLD_TIME_SECONDS = 30;
export const MAX_HOLD_TIME_SECONDS = 45;
export const MIN_CARDIO_DURATION_MINUTES = 20;
export const MAX_CARDIO_DURATION_MINUTES = 45;